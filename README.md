# nazo-hints

静的なヒント閲覧ページです。`hints.csv` を読み込んで表示します。

## 使い方

1. ルートに `hints.csv` を置く
2. `index.html` を開く（ローカルの場合）
3. Vercel にこのディレクトリをそのままデプロイ

## CSV 形式

ヘッダーは以下の3列にしてください。

- `謎`
- `ヒント段階`
- `ヒント`

`ヒント段階` に `答え` と書かれた行は「答え」として表示されます。
